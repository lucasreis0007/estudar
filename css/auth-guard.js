// OpenMind — guarda de página simples: só redireciona pro login se
// não houver ninguém autenticado. Para páginas que também precisam dos
// dados do perfil (como index.html e profile.html), use
// OPENMIND_protegerPagina diretamente em vez deste arquivo.
OPENMIND_protegerPagina(function () {});
